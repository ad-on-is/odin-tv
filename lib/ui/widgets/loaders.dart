import 'package:flutter/material.dart';

class PosterLoader extends StatelessWidget {
  const PosterLoader({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
