

class WatchedController {
  // LazyBox hiveWatched = Get.find(tag: 'hive-watched');
  // Rx<bool> watched = false.obs;
  // final Key key;
  // String boxkey = '';
  // Worker? listener;

  // WatchedController(this.key) {
  //   boxkey = key.toString().replaceAll("[<'", "").replaceAll("'>]", "");
  // }

  // @override
  // void onInit() {
  //   listener = ever(watched, (_) {
  //     update();
  //   });
  //   getWatched();
  //   super.onInit();
  // }

  // void getWatched() async {
  //   watched(await hiveWatched.get(boxkey));
  // }

  // @override
  // void onClose() {
  //   listener?.dispose();
  //   super.onClose();
  // }
}
